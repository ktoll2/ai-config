# Performance Investigation Skill

Investigates latency, CPU, memory, I/O, allocation, and scalability concerns.

Use it when performance is a reported concern. It requires measurement before
optimization and distinguishes measured bottlenecks from speculation.

See `SKILL.md` for the complete workflow.
