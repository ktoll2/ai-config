# Bug Investigation Skill

Investigates errors, regressions, failing tests, and unexpected behavior from
evidence before proposing a fix.

Use it when the root cause is uncertain. It reports observed behavior, a
confirmed root cause when available, unresolved hypotheses, a recommended fix,
and relevant tests.

See `SKILL.md` for the complete workflow.
