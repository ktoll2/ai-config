# Testing Rules

Build or type-check affected code, run focused tests, and inspect failures.
Run broader tests when practical. For bug fixes, add a test that proves the
correction where feasible. Report exactly what was and was not verified.
