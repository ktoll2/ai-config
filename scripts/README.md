# Scripts

Reserve this directory for small, documented maintenance utilities that cannot
be handled directly through the repository's configuration files.

Scripts should be portable where practical, avoid modifying user configuration
without explicit confirmation, and document their usage in the root README.
